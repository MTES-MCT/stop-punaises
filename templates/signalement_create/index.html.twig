{% extends 'base-back.html.twig' %}

{% block title %}Ajouter un signalement{% endblock %}

{% block body %}
<div class="creer-signalement">
    <div class="fr-p-5w">
        <div class="fr-container">
            <h1>Création manuelle d’un signalement et d’une intervention</h1>
            <p>
                Renseignez les détails du signalement et de votre intervention en complétant le formulaire ci-dessous.
                Une fois toutes les étapes passées, vous pourrez le valider.
            </p>
            <p class="fr-hint-text">Sauf mention contraire, “(facultatif)” dans le label, tous les champs sont obligatoires.</p>
        </div>
    </div>

    <form action="{{ path(app_name, {'uuid': uuid}) }}" method="POST" class="fr-my-5w fr-px-5w fr-container" data-ajax="{{ path('app_liste_employes') }}">
        {{ form_row(form._token) }}

        <div id="form-creer-signalement-step-1">
            {% include "signalement_create/tab-1.html.twig" %}
        </div>

        <div id="form-creer-signalement-step-2" class="fr-stepper__hidden">
            {% include "signalement_create/tab-2.html.twig" %}
        </div>
    </form>
</div>
{% endblock %}
